# 围达网参考界面分析

## 总体视觉结论

参考图表现的不是普通网页，而是“网页内容装进早期 Windows 围棋客户端”的混合界面。视觉核心是固定宽度 PC 工作区、高信息密度、暖黄棋盘、冷蓝控件、宋体中文、像素级硬边框和略微失真的显示器质感。实现时保留这一视觉 DNA，只升级浏览器兼容性、交互性能与数据架构。

## 图一：对局结束页

- 页面性质：公开棋局/观战结束状态。
- 比例：顶部菜单约占高度 4%，品牌横幅约 10%，主体约 86%；主体左右约为 64:36。
- 左侧：十九路棋盘几乎顶满区域，坐标贴近边缘，棋子密度很高，结果窗口覆盖棋盘中心约 70% 宽、35% 高。
- 右侧：上方为对局信息和双方选手，下方为“对局进程/大厅消息”分页；信息密度高，几乎没有装饰性留白。
- 色彩：棋盘为橙黄木色；站点横幅为灰白；按钮和窗口标题使用深靛蓝；消息区为旧纸张米黄。
- 字体：菜单和正文接近 14–18px 宋体；姓名约 20–24px；结果标题约 30–34px；字重主要依靠系统宋体粗体，不使用现代无衬线大标题。
- 边框：大量 `inset/outset/ridge` 双亮暗边，面板间以 2–5px 硬边分隔，没有卡片圆角。
- 按钮：早期 Windows 立体按钮；重要操作为深蓝底白字，次要按钮为灰蓝金属渐变。
- 状态：房间号、规则、贴目、胜负印章、棋手段位、战绩和聊天时间都直接露出。

## 图二：玩家主页

- 页面性质：玩家公开资料和棋局历史。
- 比例：同一软件外壳，资料内容区约占页面 82%；中轴布局，内容宽度仅占主体约 50–60%。
- 导航：最上层仍是“开始/操作/对战/设置/帮助”软件菜单，横幅下方增加暖棕色页面标题条。
- 留白：与对局页相比明显更多，围绕头像、姓名、资料表保留大块米灰留白；这是功能页差异，不应套成现代卡片。
- 资料结构：头像、用户名、签名、注册时间、段位、年龄等使用居中纵向列表；历史棋局使用横向滚动小棋盘缩略图。
- 字号：用户名约 36px，签名 16–18px，资料 14–16px，历史棋局 13–15px。
- 背景：偏灰绿的旧纸色，存在显示器曝光不均和轻微色偏。

## 图三：对局中的私聊窗口

- 页面性质：正在进行的棋局，叠加玩家私聊。
- 结构：左侧存在纵向玩家资料轨，中间是棋盘，右侧是招法/控制区；蓝色 Windows 对话框跨越棋盘主要区域。
- 信息密度：玩家头像、胜负、棋力、计时器、房间信息、招法、帮助/保存操作同时出现。
- 对话框：标题栏为高饱和蓝色，内容区为偏蓝白色；发言人和时间使用蓝/绿粗体，正文为黑色；窗口下部保留大块输入区。
- 交互启示：私聊不能是独立现代抽屉，应是可关闭的桌面式浮窗；棋盘和周边状态在其后仍可辨认。

## 图四：完整客户端远景

- 页面性质：图一的完整屏幕比例校验。
- 关键结论：应用外壳为固定 PC 比例，并未为了小屏重新排版；1440×900 和 1366×768 应优先保留横向三栏/两栏结构，窄屏允许横向滚动。
- 清晰度：实际画面有电视拍摄产生的柔焦、摩尔纹、偏色和暗角。网站实现采用极轻的模糊、降饱和、扫描线与像素背景，不直接把参考截图当背景。

## 站点级布局映射

### 顶部

1. 软件菜单条：开始、操作、对战、设置、帮助。
2. 品牌横幅：原创“围达网”字标、棋形徽记、竹叶抽象图案、账号/帮助入口。
3. 门户主导航：首页、对弈大厅、棋谱、赛事、排行榜、高手对局、围棋资讯。

### 首页

- 左栏约 23%：高手在线、玩家状态、快捷操作。
- 中栏约 52%：公告、热门棋局、最近完成棋局和房间入口。
- 右栏约 25%：排行榜、今日统计、好友状态。
- 底部状态栏：在线人数、对局数、服务器状态和本地时间。

### 对弈大厅

- 左栏约 22%：在线用户列表与状态筛选。
- 中栏约 55%：房间列表、规则/棋盘筛选、房间操作。
- 右栏约 23%：快捷匹配、公告、排行榜。
- 每个用户显示头像符号、用户名、段位、胜率和状态；每个房间显示房间号、双方/等待者、棋盘、规则、时间、状态、观战人数。

## 设计约束

- 使用宋体/仿宋优先的系统字体栈，正文 13–16px，避免营销式超大标题。
- 面板间距控制在 4–10px；依靠边框和标题条组织层级，不依靠大留白和圆角卡片。
- 主要色板：`#ede8cf` 旧纸、`#d9a252` 棋木、`#173e79` 深蓝、`#9eb9c8` 灰蓝、`#30352b` 墨绿。
- 仅使用短距离金属/木质渐变；禁止现代霓虹、玻璃拟态和大面积柔和渐变。
- 原参考中的影视角色头像和原始标识只保留结构关系，交付使用原创剪影、棋子和文字图形。
